export enum RouterMap {
  HOME = '/dashboard',
  LOGIN = '/login'
}

export enum SettingFilds {
  ACCESS_TOKEN = 'token',
  USER_INFO = 'userInfo',
  MENU = 'menu'
}

export type ThemeType = 'default' | 'black';